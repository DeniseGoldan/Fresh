// Declaring variables
variable a = 2;
variable b = 3 * 4 / (5 - 2);
constant variable c = (-3 + 4);
constant variable d = 10;
variable value = 1;

// User defined functions
variable myOwnFunction = function(var1, var2) {
	for (variable i=0; i<=n; i++)
	{
    	print(i);
    }
	return var1 + var2;
};

// User defined data type
variable myOwnDataStucture = {
	element_1 : 2,
	element_2 : 3,
	element_3 : 5,
	element_4 : "banana",
	element_5 : 4,
	element_6 : [1, "ana", [1, 2, 3]]
};

// Different types of control statements

if (a > b + 5 && (x < 2 || y < 5 + z) || x == 2) {
	print(max(a+b, 4444));
} else {
	print(max(a+b, 5555));
}

switch (value) {
	case 1:
		print(a);
	case 2:
	case 3:
		print(b);
	default:
		print(a+b);
}

// Different kind of loops
while ( a < b)
{
	a++;
	variable sum = gcd(a,b);
}

do
{
	variable i = 2;
	i++;
} while ( i <= 5);

for (variable i=0; i<=n; i++)
{
	print(i);
}
